import { EvidenziaDirective } from './evidenzia.directive';

describe('EvidenziaDirective', () => {
  it('should create an instance', () => {
    const directive = new EvidenziaDirective();
    expect(directive).toBeTruthy();
  });
});
